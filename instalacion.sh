#!/bin/bash

chmod +x $PWD/minibin/ayuda
chmod +x $PWD/minibin/busqueda
chmod +x $PWD/minibin/infosis
chmod +x $PWD/minibin/q
chmod +x $PWD/minibin/f
chmod +x $PWD/minibin/reproductor
chmod +x $PWD/minibin/s
chmod +x $PWD/minibin/creditos
chmod +x $PWD/minibin/fechayhora
chmod +x $PWD/minibin/juego
chmod +x $PWD/cancionsiguiente.sh
chmod +x $PWD/indicecancion.sh
chmod +x $PWD/reproductor.sh
chmod +x $PWD/terminal2.sh
chmod +x $PWD/login.sh






